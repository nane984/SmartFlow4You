/** Fixed public navbar height — keep header + main offset in sync. */
export const PUBLIC_NAV_HEIGHT_PX = 64;

export const PUBLIC_NAV_HEIGHT_CLASS = "h-16";

/** Top padding for main content below fixed PublicNavbar */
export const PUBLIC_MAIN_OFFSET_CLASS = "pt-16";

/** Scroll margin for in-page anchors under fixed PublicNavbar */
export const PUBLIC_SCROLL_MARGIN_CLASS = "scroll-mt-20";
